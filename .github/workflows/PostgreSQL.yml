name: PostgreSQL Backup on Push

on:
  push:  # 🚀 Dispara o backup em todo push no repositório
    branches:
      - main  # ajuste se for outro branch

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v3

      - name: 🧰 Instalar PostgreSQL Client e rclone
        run: |
          sudo apt update
          sudo apt install -y postgresql-client rclone

      - name: 📦 Dump e upload para Google Drive
        env:
          PG_HOST: ${{ secrets.PG_HOST }}
          PG_PORT: ${{ secrets.PG_PORT }}
          PG_USER: ${{ secrets.PG_USER }}
          PG_PASSWORD: ${{ secrets.PG_PASSWORD }}
          PG_DB: ${{ secrets.PG_DB }}
          RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
        run: bash script/dump_and_upload.sh
