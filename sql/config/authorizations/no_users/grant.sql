GRANT INSERT
    (
        own_id,
        own_name,
        own_last_name,
        own_value,
        own_cpf,
        own_email,
        own_password
    )
    ON sysflow.owner
    TO sysflow_no_user